name: Precompiled NIFs

on:
  push:
    tags:
      - "v*"
  pull_request:
    paths:
      - ".github/workflows/precompiled-nifs.yml"
      - "lib/mneme/native.ex"
      - "native/mneme_nif.zig"
      - "mix.exs"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build_nifs:
    name: NIF - ${{ matrix.target }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { target: aarch64-macos-none, os: macos-14, zig_arch: aarch64-macos }
          - { target: x86_64-macos-none, os: macos-14, zig_arch: aarch64-macos }
          - { target: aarch64-linux-gnu, os: ubuntu-22.04, zig_arch: x86_64-linux }
          - { target: aarch64-linux-musl, os: ubuntu-22.04, zig_arch: x86_64-linux }
          - { target: x86_64-linux-gnu, os: ubuntu-22.04, zig_arch: x86_64-linux }
          - { target: x86_64-linux-musl, os: ubuntu-22.04, zig_arch: x86_64-linux }

    env:
      ELIXIR_VERSION: "1.18.3"
      OTP_VERSION: "27.2"
      ZIG_VERSION: "0.15.2"
      MNEME_BUILD: "1"
      MNEME_NIF_BASE_URL: https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-nif-${{ matrix.target }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-nif-${{ matrix.target }}-

      - name: Install dependencies
        run: mix deps.get

      - name: Extract project version
        shell: bash
        run: |
          echo "PROJECT_VERSION=$(sed -n 's/^[[:space:]]*version: \"\\(.*\\)\".*/\\1/p' mix.exs | head -n1)" >> "$GITHUB_ENV"

      - name: Download Zig
        run: mix zig.get

      - name: Set Zigler target triple (cross-compile)
        shell: bash
        run: |
          case "${{ matrix.target }}" in
            aarch64-linux-gnu)
              echo "TARGET_ARCH=aarch64" >> "$GITHUB_ENV"
              echo "TARGET_OS=linux" >> "$GITHUB_ENV"
              echo "TARGET_ABI=gnu" >> "$GITHUB_ENV"
              ;;
            aarch64-linux-musl)
              echo "TARGET_ARCH=aarch64" >> "$GITHUB_ENV"
              echo "TARGET_OS=linux" >> "$GITHUB_ENV"
              echo "TARGET_ABI=musl" >> "$GITHUB_ENV"
              ;;
            x86_64-linux-gnu)
              echo "TARGET_ARCH=x86_64" >> "$GITHUB_ENV"
              echo "TARGET_OS=linux" >> "$GITHUB_ENV"
              echo "TARGET_ABI=gnu" >> "$GITHUB_ENV"
              ;;
            x86_64-linux-musl)
              echo "TARGET_ARCH=x86_64" >> "$GITHUB_ENV"
              echo "TARGET_OS=linux" >> "$GITHUB_ENV"
              echo "TARGET_ABI=musl" >> "$GITHUB_ENV"
              ;;
            aarch64-macos-none)
              echo "TARGET_ARCH=aarch64" >> "$GITHUB_ENV"
              echo "TARGET_OS=darwin" >> "$GITHUB_ENV"
              echo "TARGET_ABI=none" >> "$GITHUB_ENV"
              ;;
            x86_64-macos-none)
              echo "TARGET_ARCH=x86_64" >> "$GITHUB_ENV"
              echo "TARGET_OS=darwin" >> "$GITHUB_ENV"
              echo "TARGET_ABI=none" >> "$GITHUB_ENV"
              ;;
            *)
              echo "::error::Unknown matrix.target: ${{ matrix.target }}"
              exit 1
              ;;
          esac

      - name: Compile NIF from source
        env:
          MIX_ENV: dev
        run: mix compile --warnings-as-errors

      - name: Package artifact
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="${PROJECT_VERSION}"
          fi

          if [ -z "${VERSION}" ]; then
            echo "::error::Could not determine project version for artifact naming"
            echo "GITHUB_REF=${GITHUB_REF}"
            echo "GITHUB_REF_NAME=${GITHUB_REF_NAME}"
            echo "PROJECT_VERSION=${PROJECT_VERSION}"
            exit 1
          fi

          BASENAME="Elixir.Mneme.Native"
          NIF_DIR="_build/dev/lib/mneme/priv/lib"
          NIF_FILE="${NIF_DIR}/${BASENAME}.so"

          if [ ! -f "$NIF_FILE" ]; then
            echo "::error::NIF file not found: $NIF_FILE"
            find _build -maxdepth 8 -type f -name "Elixir.Mneme.Native*.so" || true
            exit 1
          fi

          ARTIFACT="${BASENAME}-v${VERSION}-${{ matrix.target }}.so.tar.gz"
          tar czf "${ARTIFACT}" -C "$NIF_DIR" .
          echo "Packaged ${ARTIFACT}"

      - name: Artifact attestation
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: "*.tar.gz"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mneme-native-v${{ env.PROJECT_VERSION }}-${{ matrix.target }}
          path: "*.tar.gz"

  release:
    name: Create release
    needs: build_nifs
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true

      - name: Create checksums
        run: |
          cd release-artifacts
          sha256sum *.tar.gz > checksums.txt

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-artifacts/*.tar.gz
            release-artifacts/checksums.txt
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish_hex:
    name: Publish to Hex.pm
    needs: release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    env:
      ELIXIR_VERSION: "1.18.3"
      OTP_VERSION: "27.2"
      MNEME_BUILD: "1"
      MNEME_NIF_BASE_URL: https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}

      - name: Install dependencies
        run: mix deps.get

      - name: Download Zig
        run: mix zig.get

      - name: Compile NIF from source
        env:
          MIX_ENV: dev
        run: mix compile --warnings-as-errors

      - name: Generate checksum file from GitHub Release
        run: mix zigler_precompiled.download Mneme.Native --all --print

      - name: Publish to Hex
        run: mix hex.publish --yes
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
