# Phase 01: Zigler

Zigler enables writing NIF implementations in Zig from Elixir projects.

This project uses Zigler for:

- native implementation in `native/mneme_nif.zig`
- integration with Elixir wrapper modules
- future precompiled artifact support via `zigler_precompiled`
